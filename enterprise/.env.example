# Enterprise Multipass Environment Configuration
# ==============================================================================
# SECURITY WARNING:
# - This file contains PLACEHOLDER values for development/testing
# - Copy this file to .env and replace ALL secrets with production values
# - NEVER commit .env to version control (.env is in .gitignore)
# - Generate strong random secrets for production deployment
# ==============================================================================

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# PostgreSQL connection string
# Format: postgresql://username:password@host:port/database
# SECURITY: Use strong unique credentials - NOT the defaults below!
# Generate password with: openssl rand -base64 24
DATABASE_URL=postgresql://CHANGE_DB_USER:CHANGE_DB_PASSWORD@localhost:5432/boundless_enterprise

# Maximum number of database connections in the pool
DATABASE_MAX_CONNECTIONS=10

# =============================================================================
# API SERVER CONFIGURATION
# =============================================================================

# Address and port to bind the API server
# Use 0.0.0.0 to listen on all interfaces
# Use 127.0.0.1 to listen only on localhost
BIND_ADDR=0.0.0.0:8080

# =============================================================================
# BLOCKCHAIN CONFIGURATION
# =============================================================================

# Boundless blockchain HTTP REST bridge endpoint
BOUNDLESS_HTTP_URL=http://localhost:9933

# =============================================================================
# AUTHENTICATION CONFIGURATION
# =============================================================================

# JWT secret key for session tokens
# CRITICAL: Generate with: openssl rand -base64 64
# MINIMUM: 256 bits of entropy required
# This placeholder WILL NOT WORK - you must generate a real secret!
JWT_SECRET=REPLACE_ME_GENERATE_WITH_OPENSSL_RAND_BASE64_64_MINIMUM_256_BITS

# JWT token expiration time (in hours)
JWT_EXPIRATION_HOURS=24

# =============================================================================
# SECURITY CONFIGURATION
# =============================================================================

# Enable 2FA requirement for all users (true/false)
REQUIRE_2FA=false

# Maximum failed login attempts before account lockout
MAX_FAILED_LOGIN_ATTEMPTS=5

# Account lockout duration (in minutes)
LOCKOUT_DURATION_MINUTES=30

# Master encryption key for keystore (AES-256-GCM)
# CRITICAL SECURITY WARNING:
# - This key encrypts ALL private keys in the database
# - If lost, encrypted keys cannot be recovered
# - If compromised, all private keys are exposed
# - PRODUCTION: Use AWS KMS, Azure Key Vault, or HashiCorp Vault
# - For local testing only: openssl rand -hex 32
# - This placeholder WILL NOT WORK - must be exactly 64 hex characters
MASTER_ENCRYPTION_KEY=REPLACE_WITH_64_HEX_CHARS_OR_USE_KMS_SEE_SECURITY_REMEDIATION_URGENT_MD

# =============================================================================
# KYC/AML CONFIGURATION
# =============================================================================

# Third-party KYC provider API key
# KYC_PROVIDER_API_KEY=your_api_key_here

# KYC verification webhook URL
# KYC_WEBHOOK_URL=https://your-domain.com/api/webhooks/kyc

# =============================================================================
# NOTIFICATION CONFIGURATION
# =============================================================================

# Email SMTP configuration
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=noreply@yourdomain.com
# SMTP_PASSWORD=your_smtp_password
# SMTP_FROM_EMAIL=noreply@yourdomain.com
# SMTP_FROM_NAME=Boundless Enterprise

# =============================================================================
# LOGGING CONFIGURATION
# =============================================================================

# Log level: trace, debug, info, warn, error
RUST_LOG=info

# Enable JSON-formatted logs (useful for production log aggregation)
LOG_JSON=false

# =============================================================================
# CORS CONFIGURATION
# =============================================================================

# Allowed origins for CORS (comma-separated)
# CORS_ALLOWED_ORIGINS=https://app.yourdomain.com,https://dashboard.yourdomain.com

# =============================================================================
# RATE LIMITING
# =============================================================================

# Maximum requests per minute per IP address
# RATE_LIMIT_PER_MINUTE=60

# =============================================================================
# HARDWARE PASS CONFIGURATION
# =============================================================================

# Hardware pass challenge expiration time (in minutes)
HARDWARE_CHALLENGE_EXPIRATION=5

# =============================================================================
# ASSET & MARKET CONFIGURATION
# =============================================================================

# Enable order matching engine (true/false)
ENABLE_ORDER_MATCHING=true

# Order matching interval (in seconds)
ORDER_MATCHING_INTERVAL_SECS=5

# =============================================================================
# REPORT GENERATION
# =============================================================================

# Maximum report generation time (in seconds)
MAX_REPORT_GENERATION_TIME=300

# Report storage path (for generated PDFs, CSVs, etc.)
REPORT_STORAGE_PATH=/var/lib/boundless/reports

# =============================================================================
# DEVELOPMENT / TESTING
# =============================================================================

# Enable development mode (disables some security checks)
DEV_MODE=false

# Enable API request logging
LOG_API_REQUESTS=true
